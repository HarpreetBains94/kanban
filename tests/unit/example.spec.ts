import { shallowMount } from '@vue/test-utils'
import KanbanComponent from '@/components/KanbanComponent.vue'

//Here I would mock the DataService and test the logic in the component
describe('KanbanComponent.vue', () => {
  it('Have the correct number of kanban groups', () => {
    //
  })
  it('Have the correct number of items in a kanban group', () => {
    //
  })
  it('Calling change event sends correct data to service', () => {
    //
  })
})
